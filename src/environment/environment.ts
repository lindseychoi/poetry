export const environment = {
    production: false,
    // for our purposes, we'll go ahead and place the poetry api as the baseUrl. Generally this would be the backend gateway or other backend service's URL
    apiUrl: 'https://poetrydb.org'
}